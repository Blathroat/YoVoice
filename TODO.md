##  待办

- 添加骰子/随机数功能和动画（若值域整数数量能形成正四/六/十二/二十面体则显示掷骰子动画），否则仅回复随机数
- 添加计分，计血功能，每次调用规则判断时作为上下文传递当前分数血量
- 添加语音合成、回复功能（前端合成，使用web speech api）
  - 进阶：使用后端调用百炼api实时语音合成更拟自定义的回复
- 完善MODEL_PROMPT
  - 建立游戏规则对应端点，返回游戏规则
  - 已有游戏通过对应路由端点返回规则，通过MCP传递给ai上下文
  - 未有游戏通过搜索引擎MCP自动搜索
- 使用模型提供的流式输出的功能，分段合成语音并输出
- 优化游戏专有名词的语音识别

## 文档参考
项目主模型：[百炼大模型-结构化输出文档](https://bailian.console.aliyun.com/?spm=a2ty02.30268951.0.0.58c274a1zjcbFk&tab=doc#/doc/?type=model&url=2862209)

## 模型返回json格式约定
首个字段名固定为`reply`，值为回复给用户的字符串  
第二个字段名固定为`activity`，值为以下字段中的一个：
- `none`：仅回复，不作其他反应，无其他字段
- `roll`：掷骰子/取随机数
  - 第三个字段为 `"lower": int`，用户不提供时默认为1
  - 第四个字段为 `"upper": int`，由这两个值确定随机数值域（闭区间）
- `scoreboard`：计分板
  - 第三个字段为 `"name": string`，值为用户给此计分板的名称（如XX的血量）
  - 第四个字段为 `"score": string`，值为计分板存储的数值